# ========================================
# BulC Homepage - 환경 변수 설정 파일 (예시)
# 이 파일을 복사하여 .env 파일로 저장하고 값을 수정하세요
# 주의: .env 파일은 절대 Git에 커밋하지 마세요!
# ========================================

# 데이터베이스 설정
DB_HOST=database
DB_NAME=bulc_homepage_db
DB_USER=postgres
DB_PASSWORD=your_secure_password_here
DB_PORT=5432

# 백엔드 설정
BACKEND_PORT=8080

# 프론트엔드 설정
FRONTEND_PORT=3000

# JWT 설정
# JWT_SECRET은 최소 64자 이상의 랜덤 문자열을 사용하세요
# 예: openssl rand -base64 64
JWT_SECRET=your_jwt_secret_key_here_minimum_64_characters_recommended
# Access Token 만료 시간 (밀리초): 10분 = 600000
JWT_ACCESS_EXPIRATION=600000
# Refresh Token 만료 시간 (밀리초): 7일 = 604800000
JWT_REFRESH_EXPIRATION=604800000

# 서버 설정
SERVER_URL=http://localhost

# ========================================
# 이메일 설정 (Gmail SMTP 예시)
# ========================================
# Gmail을 사용하려면:
# 1. Google 계정에서 2단계 인증 활성화
# 2. 앱 비밀번호 생성 (https://myaccount.google.com/apppasswords)
# 3. 생성된 16자리 비밀번호를 MAIL_PASSWORD에 입력
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=your-email@gmail.com
MAIL_PASSWORD=your-app-password
MAIL_FROM=BulC Homepage <your-email@gmail.com>
# 이메일 발송 활성화 (false: 로그에만 기록, true: 실제 발송)
MAIL_ENABLED=false

# ========================================
# OAuth 소셜 로그인 설정
# ========================================
# 네이버 개발자센터: https://developers.naver.com
NAVER_CLIENT_ID=your_naver_client_id
NAVER_CLIENT_SECRET=your_naver_client_secret

# 카카오 개발자센터: https://developers.kakao.com
KAKAO_CLIENT_ID=your_kakao_client_id
KAKAO_CLIENT_SECRET=your_kakao_client_secret

# 구글 클라우드 콘솔: https://console.cloud.google.com
GOOGLE_CLIENT_ID=your_google_client_id
GOOGLE_CLIENT_SECRET=your_google_client_secret
